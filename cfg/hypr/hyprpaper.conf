wallpaper {
    monitor =
    path = ~/.config/hypr/wallpapers/rainier-panorama.webp
    # Show the whole panorama at each monitor's full width. The unused height
    # is left blank rather than cropping or distorting the image.
    fit_mode = contain
}

splash = false
