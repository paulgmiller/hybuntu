general {
    hide_cursor = false
    ignore_empty_input = true
}

background {
    monitor =
    color = rgb(24, 24, 24)
}

# Clock
label {
    monitor =
    text = $TIME
    color = rgba(255, 255, 255, 1.0)
    font_size = 72
    font_family = JetBrains Mono

    position = 0, 180
    halign = center
    valign = center
}

# Username
label {
    monitor =
    text = $USER
    color = rgba(220, 220, 220, 1.0)
    font_size = 20
    font_family = JetBrains Mono

    position = 0, 70
    halign = center
    valign = center
}

# Password box
input-field {
    monitor =
    size = 300, 50
    outline_thickness = 2

    outer_color = rgba(255, 255, 255, 0.7)
    inner_color = rgba(40, 40, 40, 1.0)
    font_color = rgba(255, 255, 255, 1.0)
    font_family = JetBrains Mono

    placeholder_text = <i>Password...</i>
    fail_text = <i>$FAIL</i>

    dots_center = true
    fade_on_empty = false
    rounding = 10

    position = 0, 0
    halign = center
    valign = center
}

# GDM's greeter is on tty1. Switching VTs leaves this session locked and
# running so it can be resumed later.
label {
    monitor =
    text = Switch user: Ctrl + Alt + F1
    color = rgba(190, 190, 190, 1.0)
    font_size = 16
    font_family = JetBrains Mono

    position = 0, -80
    halign = center
    valign = center
}
